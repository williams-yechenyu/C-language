#include <stdio.h>
int main()
{
int add(int num1,int num2);
int a,b,sum=0;
printf("a=1,b=2");
scanf("%d%d",&a,&b);
sum=add(a,b);
printf("a+b=%d\n",sum);
return 0;
}
int add(int num1,int num2)
{
int total=0;
total=num1+num2;
return(total);
}
